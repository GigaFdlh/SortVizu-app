class AppSizes {
  AppSizes._();

  // ========== SPACING (8pt Grid) ==========
  
  /// Extra small - 4dp (icon padding, tiny gaps)
  static const double xs = 4.0;
  
  /// Small - 8dp (compact spacing)
  static const double sm = 8.0;
  
  /// Medium - 16dp (DEFAULT spacing) ⭐
  static const double md = 16.0;
  
  /// Large - 24dp (section spacing, screen padding)
  static const double lg = 24.0;
  
  /// Extra large - 32dp (major sections)
  static const double xl = 32.0;
  
  /// Extra extra large - 48dp (screen margins)
  static const double xxl = 48.0;

  // ========== BORDER RADIUS (Rounded Corners) ==========
  
  /// Extra small radius - 4dp
  static const double radiusXs = 4.0;
  
  /// Small radius - 8dp (subtle rounding)
  static const double radiusSm = 8.0;
  
  /// Medium radius - 12dp (DEFAULT) ⭐
  static const double radiusMd = 12.0;
  
  /// Large radius - 16dp (cards, panels)
  static const double radiusLg = 16.0;
  
  /// Extra large radius - 24dp (sheets, modals)
  static const double radiusXl = 24.0;
  
  /// Full radius - 9999dp (circular)
  static const double radiusFull = 9999.0;

  // ========== ICON SIZES ==========
  
  /// Small icon - 16dp
  static const double iconSm = 16.0;
  
  /// Medium icon - 24dp (DEFAULT) ⭐
  static const double iconMd = 24.0;
  
  /// Large icon - 32dp
  static const double iconLg = 32.0;
  
  /// Extra large icon - 48dp (feature icons)
  static const double iconXl = 48.0;

  // ========== BUTTON SIZES ==========
  
  /// Small button height - 36dp
  static const double buttonHeightSm = 36.0;
  
  /// Medium button height - 48dp (DEFAULT, touch-friendly) ⭐
  static const double buttonHeightMd = 48.0;
  
  /// Large button height - 56dp
  static const double buttonHeightLg = 56.0;
  
  /// Button padding horizontal - 24dp
  static const double buttonPaddingH = 24.0;
  
  /// Button padding vertical - 12dp
  static const double buttonPaddingV = 12.0;

  // ========== TOUCH TARGETS (Mobile-First) ==========
  
  /// Minimum touch target - 48dp (Material Design guideline)
  static const double touchTargetMin = 48.0;
  
  /// Comfortable touch target - 56dp
  static const double touchTargetComfortable = 56.0;

  // ========== BAR VISUALIZATION ==========
  
  /// Minimum bar width - 2dp (for large arrays)
  static const double barMinWidth = 2.0;
  
  /// Maximum bar width - 12dp (for small arrays)
  static const double barMaxWidth = 12.0;
  
  /// Bar spacing - 1dp (gap between bars)
  static const double barSpacing = 1.0;
  
  /// Bar border radius - 3dp (subtle rounding)
  static const double barRadius = 3.0;

  // ========== ELEVATION (Shadow Depth) ==========
  
  /// Small elevation - 2dp
  static const double elevationSm = 2.0;
  
  /// Medium elevation - 4dp (DEFAULT) ⭐
  static const double elevationMd = 4.0;
  
  /// Large elevation - 8dp (prominent elements)
  static const double elevationLg = 8.0;
  
  /// Extra large elevation - 16dp (modals, dialogs)
  static const double elevationXl = 16.0;

  // ========== APPBAR ==========
  
  /// AppBar height - 56dp (standard)
  static const double appBarHeight = 56.0;
  
  /// AppBar elevation - 0dp (flat, modern)
  static const double appBarElevation = 0.0;

  // ========== FAB (Floating Action Button) ==========
  
  /// FAB size - 56dp (standard)
  static const double fabSize = 56.0;
  
  /// FAB margin - 16dp (from edges)
  static const double fabMargin = 16.0;
  
  /// FAB elevation - 6dp
  static const double fabElevation = 6.0;

  // ========== CONTROL PANEL ==========
  
  /// Control panel height - 200dp (comfortable for sliders + buttons)
  static const double controlPanelHeight = 200.0;
  
  /// Control panel padding - 20dp
  static const double controlPanelPadding = 20.0;
  
  /// Slider height (including touch area) - 48dp
  static const double sliderHeight = 48.0;

  // ========== STATS DISPLAY ==========
  
  /// Stats bar height - 60dp
  static const double statsBarHeight = 60.0;
  
  /// Stats item gap - 12dp
  static const double statsItemGap = 12.0;

  // ========== CARDS ==========
  
  /// Card padding - 16dp
  static const double cardPadding = 16.0;
  
  /// Card margin - 8dp
  static const double cardMargin = 8.0;
  
  /// Card elevation - 2dp
  static const double cardElevation = 2.0;

  // ========== DIVIDER ==========
  
  /// Divider thickness - 1dp
  static const double dividerThickness = 1.0;
  
  /// Divider indent - 16dp
  static const double dividerIndent = 16.0;

  // ========== ANIMATION DURATIONS (in milliseconds) ==========
  
  /// Quick animation - 100ms (micro-interactions)
  static const int durationQuick = 100;
  
  /// Short animation - 200ms (button press, color change)
  static const int durationShort = 200;
  
  /// Medium animation - 300ms (DEFAULT, smooth transitions) ⭐
  static const int durationMedium = 300;
  
  /// Long animation - 500ms (bouncy, playful)
  static const int durationLong = 500;
  
  /// Extra long animation - 800ms (celebration, emphasis)
  static const int durationXLong = 800;
}